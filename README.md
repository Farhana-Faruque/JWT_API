# JWT_API

A simple **JSON Web Token (JWT) based authentication API** built with **Node.js** and **Express**.

This project demonstrates how to implement JWT authentication, including user login, token issuance, and middleware protection for routes.

---

## 🚀 Features

- User authentication using JWTs
- Protected routes using auth middleware
- Simple Express server setup
- Example user model (in-memory or dummy data)
- Token validation and error handling

---

## 🛠️ Technologies

- Node.js
- Express
- JSON Web Tokens (JWT)
- JavaScript

---

## 📦 Installation

1. **Clone the repository**
   ```bash
   git clone https://github.com/Farhana-Faruque/JWT_API.git
   cd JWT_API
